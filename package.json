{
  "name": "design-md-generator",
  "version": "1.0.0",
  "description": "Export Figma variables and styles to DESIGN.md — open agent-agnostic design context for Cursor, Claude, and any AI agent",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
